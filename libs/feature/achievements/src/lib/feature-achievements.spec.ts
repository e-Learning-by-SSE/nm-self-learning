import { featureAchievements } from "./feature-achievements";

describe("featureAchievements", () => {
	it("should work", () => {
		expect(featureAchievements()).toEqual("feature-achievements");
	});
});
